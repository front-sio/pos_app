// Mobile stub - not used on mobile since we use connectivity_plus
bool isOnline() {
  return true;
}

void addOnlineListener(Function() callback) {
  // Not used on mobile
}

void addOfflineListener(Function() callback) {
  // Not used on mobile
}
