version: '3.9'
services:
  flutter-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8090:80" # Expose Flutter web at http://your-vps-ip:8090
    restart: unless-stopped